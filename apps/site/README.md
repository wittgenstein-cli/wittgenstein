# Wittgenstein — official site

Next.js + Tailwind landing for the monorepo.

```bash
# from repository root
pnpm dev:site
```

Open the URL printed by Next (typically `http://localhost:3000`). Production deploy is left to your hosting; links on the homepage point at the canonical GitHub tree for docs and CLI.

node_modules/
.pnpm-store/
dist/
packages/cli/npm-publish/
build/
.next/
out/
_vercel-kimi-out/

*.tsbuildinfo
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log

.env
.env.local
.env.*.local
!.env.example

.tmp-one-click-clone/

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.sample

# Per-developer AI tooling configs — keep them local, never commit
.claude/
.codex/
.cursor/

# Python bytecode (any surface)
**/__pycache__/
**/*.pyc

coverage/
.nyc_output/

artifacts/runs/*
artifacts/benchmarks/*
artifacts/logs/*
!artifacts/runs/.gitkeep
!artifacts/benchmarks/.gitkeep
!artifacts/logs/.gitkeep

# Local image-adapter training outputs (keep raw data private / large)
data/image_adapter/train/*.jsonl
data/image_adapter/artifacts/

python/image_adapter/.venv/

# polyglot-mini: large downloaded caption JSON + local outputs (keep scripts in git)
polyglot-mini/train/coco/*.json
polyglot-mini/outputs/
polyglot-mini/**/__pycache__/
polyglot-mini/**/*.pyc

# Optional: large trained weights (uncomment if you do not want npz in repo)
# polyglot-mini/train/adapter.npz

# Mac Finder duplicate suffix patterns
* copy*
*\ copy*
